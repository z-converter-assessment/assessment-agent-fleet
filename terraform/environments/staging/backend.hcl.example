# Copy to backend.hcl. backend.hcl 는 .gitignore 차단.
# state backend 는 local. path 는 iac 에 attach 한 cinder volume mount 경로.
# 결정 history: docs/adr/0005-terraform-state-backend.md

path = "/var/lib/terraform-state/staging/terraform.tfstate"
